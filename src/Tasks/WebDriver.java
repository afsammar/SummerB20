package Tasks;

public class WebDriver {
    public String getURL(){
        return"Opening the URL in the default browser";
    }
    public String close(){
        return "Closes the default browser";
    }
}
